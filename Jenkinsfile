pipeline{
  agent any
  
  stages {
    stage('拉取git-hub 程式') {
      steps {
				// 待放指令
      }
    }
    stage('通過maven構建項目') {
      steps {
				// 待放指令
      }
    }
    stage('通過sonarqube做代碼檢測') {
      steps {
				// 待放指令
      }
    }
    stage('通過Docker構建image') {
      steps {
				// 待放指令
      }
    }
    stage('將image推送到harbor') {
      steps {
				// 待放指令
      }
    }
  }
}